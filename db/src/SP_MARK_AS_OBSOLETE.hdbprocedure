PROCEDURE "SP_MARK_AS_OBSOLETE" (
    IN IN_ASID NVARCHAR(36), 
    IN IN_REASONCOMMENT NVARCHAR(50)
)
LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER 
AS
BEGIN
    UPDATE "BTP_CONROLLER_ASSESSMENTS" 
    SET ISOBSOLETE = TRUE , REASONFOROBSOLETE = :IN_REASONCOMMENT
    WHERE ID = :IN_ASID;
END;
